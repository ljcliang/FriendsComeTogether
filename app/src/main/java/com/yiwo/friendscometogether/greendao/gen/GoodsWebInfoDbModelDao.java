package com.yiwo.friendscometogether.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.yiwo.friendscometogether.dbmodel.GoodsWebInfoDbModel;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "GOODS_WEB_INFO_DB_MODEL".
*/
public class GoodsWebInfoDbModelDao extends AbstractDao<GoodsWebInfoDbModel, Long> {

    public static final String TABLENAME = "GOODS_WEB_INFO_DB_MODEL";

    /**
     * Properties of entity GoodsWebInfoDbModel.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Good_id = new Property(1, String.class, "good_id", false, "GOOD_ID");
        public final static Property Web_info = new Property(2, String.class, "web_info", false, "WEB_INFO");
    }


    public GoodsWebInfoDbModelDao(DaoConfig config) {
        super(config);
    }
    
    public GoodsWebInfoDbModelDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"GOODS_WEB_INFO_DB_MODEL\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"GOOD_ID\" TEXT," + // 1: good_id
                "\"WEB_INFO\" TEXT);"); // 2: web_info
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"GOODS_WEB_INFO_DB_MODEL\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, GoodsWebInfoDbModel entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String good_id = entity.getGood_id();
        if (good_id != null) {
            stmt.bindString(2, good_id);
        }
 
        String web_info = entity.getWeb_info();
        if (web_info != null) {
            stmt.bindString(3, web_info);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, GoodsWebInfoDbModel entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String good_id = entity.getGood_id();
        if (good_id != null) {
            stmt.bindString(2, good_id);
        }
 
        String web_info = entity.getWeb_info();
        if (web_info != null) {
            stmt.bindString(3, web_info);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public GoodsWebInfoDbModel readEntity(Cursor cursor, int offset) {
        GoodsWebInfoDbModel entity = new GoodsWebInfoDbModel( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // good_id
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2) // web_info
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, GoodsWebInfoDbModel entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setGood_id(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setWeb_info(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(GoodsWebInfoDbModel entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(GoodsWebInfoDbModel entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(GoodsWebInfoDbModel entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
